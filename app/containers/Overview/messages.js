/*
 * Overview Messages
 *
 * This contains all the text for the Overview component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.Overview';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is Overview container !',
  },
});
